function myFunction(event) {
    event.preventDefault()
    if (event.target.classList.contains("slow")) {
      
    }
    if (event.target.classList.contains("slowmedium")) {
        
    }
    if (event.target.classList.contains("medium")) {
        
    }
    if (event.target.classList.contains("mediumbusy")) {
        
    }
    if (event.target.classList.contains("busy")) {
        
    }

  }